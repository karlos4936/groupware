<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.Data">

<!-- 	자료실 리스트 -->
	<select id="list" resultType="com.dk.groupware.data.model.Data">
		select no, title, writer, to_char(wdate, 'yyyy-mm-dd') wdate, hit
		from data order by no desc
	</select>
<!-- 	글의 총갯수 -->
	<select id="selectCount" resultType="Integer">
		select count(no) from data
<!-- 		<if test="searchWord!=null"> -->
<!-- 			where #{searchKey} like '%#{searchWord}%' -->
<!-- 		</if> -->
	</select>
<!-- 	게시판 글보기 -->
	<select id="view" resultType="com.dk.groupware.data.model.Data">
		select no, title, content, writer, hit, fileName from data
		where no=#{no}
	</select>
	
<!-- 	게시판 글쓰기 -->
	<insert id="write">
		insert into data(no, title, content, writer, fileName)
		values(data_seq.nextval, #{title}, #{content}, #{writer},#{fileName})
	</insert>
	
<!-- 	게시판 글수정 -->
  <update id="update">
  	update data set title=#{title},content=#{content}
  	<if test="fileName!=null">, fileName=#{fileName}</if> where no=#{no}
  </update>
  
<!--   게시판 글삭제 -->
  <delete id="delete">
  	delete from data where no=#{no}
  </delete>
  
<!-- 	조회수 증가 -->
<update id="hit">
	update data set hit=hit+1 where no=#{no}
</update>
</mapper>